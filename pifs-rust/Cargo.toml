[package]
name = "pifs"
version = "0.1.0"
edition = "2021"
description = "FUSE filesystem using IPFS as backend storage"
license = "GPL-3.0"

[[bin]]
name = "pifs"
path = "src/main.rs"

[dependencies]
fuser = "0.15"
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
nix = { version = "0.29", features = ["fs", "dir", "user"] }
libc = "0.2"
